<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Brighting Solutions | Portal Interno</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #6366f1;
            --secondary: #a855f7;
            --dark: #0f172a;
            --glass: rgba(255, 255, 255, 0.85);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
            background: var(--dark);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            overflow: hidden;
            color: #1e293b;
        }

        /* --- Fondo Digital Aura (Sustituye al Canvas pesado) --- */
        .bg-aura {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: radial-gradient(circle at 20% 30%, #4f46e5 0%, transparent 40%),
                        radial-gradient(circle at 80% 70%, #db2777 0%, transparent 40%),
                        radial-gradient(circle at 50% 50%, #7c3aed 0%, transparent 50%);
            filter: blur(80px);
            z-index: -1;
            animation: auraMove 20s ease infinite alternate;
        }

        @keyframes auraMove {
            from { transform: scale(1) translate(0, 0); }
            to { transform: scale(1.2) translate(5%, 5%); }
        }

        /* --- Contenedor Principal --- */
        .main-container {
            background: var(--glass);
            backdrop-filter: blur(15px);
            border-radius: 24px;
            padding: 50px 40px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            max-width: 550px;
            width: 100%;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.3);
            position: relative;
        }

        .logo-box {
            background: white;
            width: 80px; height: 80px;
            margin: 0 auto 20px;
            border-radius: 20px;
            display: flex; align-items: center; justify-content: center;
            font-size: 2.5rem;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        }

        .brand-title {
            font-size: 2.8rem;
            font-weight: 800;
            letter-spacing: -1.5px;
            line-height: 1;
            margin-bottom: 5px;
            background: linear-gradient(to right, #4f46e5, #9333ea);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .brand-subtitle {
            font-size: 1.2rem;
            font-weight: 500;
            color: #64748b;
            letter-spacing: 2px;
            text-transform: uppercase;
            margin-bottom: 40px;
        }

        /* --- Botones Estilo Dashboard --- */
        .buttons-container {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .btn {
            display: flex;
            align-items: center;
            padding: 18px 25px;
            text-decoration: none;
            border-radius: 16px;
            font-weight: 600;
            font-size: 1.05rem;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .btn i { width: 30px; font-size: 1.3rem; margin-right: 15px; }

        .btn:hover {
            transform: translateY(-3px) scale(1.02);
            box-shadow: 0 15px 30px -5px rgba(0, 0, 0, 0.2);
        }

        .btn-combined { background: #334155; } /* Slate 700 */
        .btn-live { background: #6366f1; }     /* Indigo 500 */
        .btn-legacy { background: #10b981; }   /* Emerald 500 */
        .btn-sales { background: #f59e0b; }    /* Amber 500 */
        .btn-admin { 
            background: white; 
            color: #1e293b; 
            margin-top: 15px; 
            border: 1px solid #e2e8f0;
        }

        .btn-admin:hover { background: #f1f5f9; color: var(--primary); }

        .footer {
            margin-top: 40px;
            font-size: 0.85rem;
            color: #94a3b8;
            font-weight: 500;
        }

        @media (max-width: 480px) {
            .main-container { padding: 30px 20px; }
            .brand-title { font-size: 2.2rem; }
        }
    </style>
</head>
<body>

    <div class="bg-aura"></div>

    <div class="main-container">
        <div class="logo-box">
            <i class="fas fa-microchip" style="color: #6366f1;"></i>
        </div>
        
        <h1 class="brand-title">BRIGHTING</h1>
        <p class="brand-subtitle">Solutions</p>
        
        <div class="buttons-container">
            <a href="{{ url_for('suppression_search') }}" class="btn btn-combined">
                <i class="fas fa-magnifying-glass-plus"></i>
                Combined Master Search
            </a>

            <a href="{{ url_for('tcpa_search') }}" class="btn btn-live">
                <i class="fas fa-bolt"></i>
                DNC 2026 Live Search
            </a>

            <a href="{{ url_for('dnc_search') }}" class="btn btn-legacy">
                <i class="fas fa-shield-halved"></i>
                Legacy DNC-Only
            </a>

            <a href="{{ url_for('sales_search') }}" class="btn btn-sales">
                <i class="fas fa-sack-dollar"></i>
                Sales Records Search
            </a>
            
            <a href="{{ url_for('admin_login') }}" class="btn btn-admin">
                <i class="fas fa-user-gear"></i>
                Admin Dashboard
            </a>
        </div>
        
        <div class="footer">
            <p><i class="fas fa-code"></i> IT Department</p>
            <p>© 2025-2026 Brighting Solutions</p>
        </div>
    </div>

</body>
</html>

